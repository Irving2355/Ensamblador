#include <stdio.h>
#include <windows.h>

extern int suma(int, int);
extern int resta(int, int);
extern int multiplica(int, int);
extern int modulo(int, int);

int main(){
    SetConsoleOutputCP(65001);
    
    int a,b,opcion,resultado;

    resultado=suma(a,b);

    if(resultado == -1){
        
    }
    
    

    return 0;
}